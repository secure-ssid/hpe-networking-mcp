oauth:
  token_url: /oauth2/token
  client_id: benchmark-client
  client_secret: benchmark-secret
  audience: aruba-central.example.com
  # Token issuance and expiry are simulated by the harness (see `runner.py`),
  # this file only binds the token request to the real client flow.